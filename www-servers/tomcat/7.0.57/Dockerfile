FROM DOCKER_BASE/dev-java/openjdk/jre7

MAINTAINER tsaikd "tsaikd@gmail.com"

ENV DOCKER_SRC
ENV TOMCAT_MAJOR 7
ENV TOMCAT_VERSION 7.0.57
ENV TOMCAT_HOME /usr/local/tomcat
ENV PATH $TOMCAT_HOME/bin:$PATH

EXPOSE 8080

ADD . $DOCKER_SRC

RUN

ENTRYPOINT

